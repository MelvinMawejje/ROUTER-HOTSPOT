# ══════════════════════════════════════════════════════════════════
# MBUYA WIFI — environment configuration
# ══════════════════════════════════════════════════════════════════
# This file holds real secrets. NEVER commit it to git, NEVER paste
# it into a chat, email, or ticket, and NEVER upload it anywhere you
# wouldn't upload your mobile money PIN.
# ══════════════════════════════════════════════════════════════════

# ── IOTEC mobile money (payment collection) ─────────────────────────
# Whoever has these can move money through your wallet. Treat like a
# bank password.
IOTEC_CLIENT_ID=pay-019e9c6e-0cda-775d-b88e-40687853599c
IOTEC_CLIENT_SECRET=IO-NJs73yg0NVd6vMSOFaLn3a2NPDeXYUmnD
IOTEC_WALLET_ID=019ed5d9-2653-7521-9ede-b99f59001c9e

# ── MikroTik router REST API credentials ────────────────────────────
# Whoever has these has full admin control of your router.
ROUTER_HOST=10.0.0.2
ROUTER_USER=melvin
ROUTER_PASS=admin

# ── RADIUS shared secret ─────────────────────────────────────────────
# Must match the secret configured on the MikroTik router's RADIUS
# client settings exactly.
RADIUS_SECRET=mbuyawifi-secret

# ── Admin dashboard PIN (protects /api/admin/metrics) ────────────────
METRICS_PIN=1234

# ── App server ────────────────────────────────────────────────────────
PORT=3000

# ── Public API base the captive portal (login.html/status.html) calls ─
# Not a secret, but centralizing it here means you change it in one
# place if the server ever moves IP.
API_BASE=http://139.84.226.141:3000
